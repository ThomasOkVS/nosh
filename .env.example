POSTGRES_USER=nosh
POSTGRES_PASSWORD=nosh
POSTGRES_DB=nosh

DATABASE_URL=postgres://nosh:nosh@localhost:5432/nosh
TEST_DATABASE_URL=postgres://nosh:nosh@localhost:5432/nosh_test

SESSION_SECRET=change-me-to-a-long-random-string

PORT=3001
VITE_API_URL=http://localhost:3001

# Backend-only (never a VITE_* var — it must not reach the browser). Powers
# recipe import from a URL; get a free key at https://aistudio.google.com/apikey.
# Leave unset and /import returns 503 — the rest of the app works fine.
GEMINI_API_KEY=
