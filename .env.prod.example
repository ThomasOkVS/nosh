# Copy to .env next to docker-compose.prod.yml on the homelab box and fill
# in real values. Never commit the filled-in .env — it's already excluded
# via .gitignore/.dockerignore.

POSTGRES_USER=nosh
POSTGRES_PASSWORD=
POSTGRES_DB=nosh

# Long random string — e.g. `openssl rand -hex 32`. Signs the session
# cookie; anyone with this value could forge a session.
SESSION_SECRET=

# The frontend's real origin as the browser will see it, e.g.
# http://nosh.<your-tailnet>.ts.net:8080 (Tailscale MagicDNS name, no TLS —
# see docs/deployment.md). Must match exactly (scheme + host + port) or the
# backend's CORS check rejects the frontend's requests.
FRONTEND_ORIGIN=

# Backend-only (never a VITE_* var — it must not reach the browser). Powers
# recipe import from a URL; get a free key at https://aistudio.google.com/apikey.
# Leave unset and /import returns 503 — the rest of the app works fine.
GEMINI_API_KEY=

# Image tag to run. "latest" (the default if unset) tracks whatever CI last
# published from main — what Watchtower polls for updates. Pin to a specific
# tag (a short git SHA) to hold back a version instead.
TAG=latest
